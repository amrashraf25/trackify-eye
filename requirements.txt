# Excel → Word Generator — Python dependencies
# Install with:  pip install -r requirements.txt

# Core
pandas>=2.0.0
openpyxl>=3.1.0          # pandas Excel engine (.xlsx read/write)
python-docx>=1.1.0        # Word document manipulation
watchdog>=4.0.0           # File-system change monitoring

# Optional — PDF export (requires Microsoft Word on Windows/macOS)
# Uncomment the line below if you want PDF export via MS Word:
# docx2pdf>=0.1.8

# Optional — freeze to .exe with PyInstaller
# pyinstaller>=6.0.0
